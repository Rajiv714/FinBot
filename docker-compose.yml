version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage
    environment:
      - QDRANT__SERVICE__HTTP_PORT=6333
      - QDRANT__SERVICE__GRPC_PORT=6334
    restart: unless-stopped

  # Optional: If you want to run the entire FinBot in Docker
  # finbot:
  #   build: .
  #   ports:
  #     - "8501:8501"
  #   depends_on:
  #     - qdrant
  #   environment:
  #     - QDRANT_HOST=qdrant
  #     - QDRANT_PORT=6333
  #   volumes:
  #     - ./Data:/app/Data
  #     - ./Llama-3.1-8B:/app/Llama-3.1-8B
  #   restart: unless-stopped